Flask==3.1.3
openpyxl==3.1.5
Werkzeug==3.1.6
python-dotenv==1.2.2

# Blindagem do parser XML do openpyxl (XXE, expansão de entidades). O openpyxl
# NÃO declara esta dependência: ele a importa se existir e, se não existir,
# volta silenciosamente ao ElementTree sem proteção. Como o app abre .xlsx que
# vieram de fora, a blindagem é obrigatória — por isso ela é fixada aqui.
defusedxml==0.7.1
# Geracao de PDF do relatorio consolidado. Puro Python: nao exige GTK,
# wkhtmltopdf nem Excel instalado — o app continua rodando offline num
# duplo clique. Desenha tabelas paginadas e graficos vetoriais nativos.
reportlab==5.0.0
